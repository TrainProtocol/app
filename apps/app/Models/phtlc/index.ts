// Re-exported from @train-protocol/sdk — single source of truth
export type {
    UserLockParams,
    LockParams,
    OldLockParams,
    RefundParams,
    RedeemSolverParams,
    GetCommitsParams,
} from '@train-protocol/sdk'
