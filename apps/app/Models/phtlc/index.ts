// Re-exported from @train-protocol/sdk — single source of truth
export type {
    CreateHTLCParams,
    LockParams,
    OldLockParams,
    RefundParams,
    ClaimParams,
    GetCommitsParams,
} from '@train-protocol/sdk'
