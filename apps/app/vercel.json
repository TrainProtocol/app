{
    "installCommand": "pnpm install",
    "buildCommand": "cd ../.. && pnpm build:packages && cd apps/app && pnpm build",
    "framework": "nextjs",
    "git": {
      "deploymentEnabled": {
        "main": true
      }
    },
    "ignoreCommand": "bash -c 'cd ../.. && git diff --quiet HEAD^ HEAD -- apps/app packages || exit 1'"
  }